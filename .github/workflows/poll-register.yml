name: Poll Registrations

on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes
  workflow_dispatch:

permissions:
  contents: write

jobs:
  poll:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Poll #register for new usernames
        env:
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
        run: python3 leaderboard.py poll
      - name: Commit processed IDs
        run: |
          git config user.name "mf-bot"
          git config user.email "bot@missionfaang.local"
          git add data/processed_ids.json data/members.json
          if git diff --cached --quiet; then echo "nothing to commit"; else git commit -m "registration update [skip ci]" && git push; fi
