{
  "Operating Systems": [
    {"q": "What is the difference between a process and a thread?", "hint": "address space, resources, scheduling unit"},
    {"q": "What happens during a context switch and what makes it expensive?", "hint": "register state, TLB/cache pollution"},
    {"q": "Process states: explain the transition diagram.", "hint": "new, ready, running, waiting, terminated"},
    {"q": "What is a zombie process and how do you prevent one?", "hint": "wait(), SIGCHLD, orphan vs zombie"},
    {"q": "Explain user mode vs kernel mode.", "hint": "privilege rings, syscall gate"},
    {"q": "What is a system call? Walk through what happens when you call read().", "hint": "trap, mode switch, kernel buffer"},
    {"q": "Compare preemptive and cooperative multitasking.", "hint": "who yields control"},
    {"q": "Explain FCFS, SJF, Round Robin and priority scheduling.", "hint": "convoy effect, starvation, quantum choice"},
    {"q": "What is priority inversion and how does priority inheritance fix it?", "hint": "Mars Pathfinder story"},
    {"q": "Race condition: define it and give a real example.", "hint": "check-then-act on shared state"},
    {"q": "Mutex vs semaphore — when would you use each?", "hint": "ownership vs counting, signalling"},
    {"q": "Explain the producer-consumer problem and its solution.", "hint": "bounded buffer, two semaphores + mutex"},
    {"q": "What are the four conditions for deadlock?", "hint": "mutual exclusion, hold-and-wait, no preemption, circular wait"},
    {"q": "How does the banker's algorithm avoid deadlock?", "hint": "safe sequence lookahead"},
    {"q": "Spinlock vs mutex: when is busy-waiting actually better?", "hint": "short critical sections, multicore"},
    {"q": "What is thrashing in virtual memory?", "hint": "page-fault storm, working set exceeded"},
    {"q": "Paging vs segmentation — compare.", "hint": "fixed vs variable size, external fragmentation"},
    {"q": "Walk through an address translation with a TLB hit and a miss.", "hint": "VPN lookup, page table walk, TLB fill"},
    {"q": "FIFO vs LRU vs Clock page replacement — tradeoffs.", "hint": "Belady's anomaly, reference bit approximation"},
    {"q": "What is demand paging and why is it better than eager loading?", "hint": "page fault on first touch, lazy allocation"},
    {"q": "Copy-on-write: how do fork() and COW interact?", "hint": "shared pages until write, then fault+copy"},
    {"q": "Internal vs external fragmentation.", "hint": "wasted inside blocks vs between blocks"},
    {"q": "Explain memory-mapped files and when they beat read()/write().", "hint": "single copy, random access, shared pages"},
    {"q": "What is the difference between a hard link and a symbolic link?", "hint": "inode refcount vs path redirection"},
    {"q": "Inodes: what do they store and what happens when they run out?", "hint": "metadata, no filename, ENOSPC despite free space"},
    {"q": "Journaling filesystems: what problem do they solve?", "hint": "crash consistency, write-ahead log"},
    {"q": "I/O: buffered vs unbuffered, blocking vs non-blocking vs async.", "hint": "who waits and where data sits"},
    {"q": "What is DMA and why does it matter for I/O performance?", "hint": "device writes RAM directly, CPU freed"},
    {"q": "Monolithic kernel vs microkernel vs hybrid.", "hint": "drivers in-kernel vs message-passing services"},
    {"q": "How does fork()+exec() implement a shell pipeline?", "hint": "pipe fds, dup2 wiring across children"},
    {"q": "What is a signal? How do you make a handler async-safe?", "hint": "reentrancy, sigaction flags, mask"},
    {"q": "Explain futexes and how mutexes are built on them.", "hint": "fast path in userspace, kernel sleep on contention"},
    {"q": "What is cache coherence and why do cores need it?", "hint": "MESI protocol, invalidations"},
    {"q": "False sharing: what is it and how do you fix it?", "hint": "same cache line, pad/align per-core data"},
    {"q": "Why can two threads incrementing a counter still lose updates under -O2?", "hint": "read-modify-write races, compiler reordering, atomics"},
    {"q": "What does volatile mean in C, and why is it NOT a synchronization tool?", "hint": "no reordering guarantees, no atomicity"},
    {"q": "Memory barrier / fence: what guarantee does it add?", "hint": "ordering of loads/stores across cores"},
    {"q": "Explain the O(1) scheduler idea vs CFS in Linux.", "hint": "red-black tree, virtual runtime fairness"},
    {"q": "What happens between power-on and the login prompt?", "hint": "BIOS/UEFI, bootloader, kernel init, init system"},
    {"q": "Container namespaces and cgroups: what do they isolate and limit?", "hint": "pid/net/mnt namespaces, cpu/mem quotas"}
  ],
  "Computer Networks": [
    {"q": "OSI vs TCP/IP model — map the layers to real protocols.", "hint": "7 vs 4 layers, ethernet/ip/tcp/http"},
    {"q": "What actually happens when you type a URL and press enter?", "hint": "DNS, ARP, TCP handshake, TLS, HTTP"},
    {"q": "Why does TCP need a three-way handshake instead of two?", "hint": "both sides confirm ISNs, stale duplicate protection"},
    {"q": "TCP vs UDP: pick the right one for video calls, DNS, file transfer.", "hint": "reliability vs latency, head-of-line blocking"},
    {"q": "How does TCP achieve reliability?", "hint": "sequence numbers, ACKs, retransmission timers"},
    {"q": "Explain flow control via sliding window.", "hint": "receiver window, advertised rwnd"},
    {"q": "Congestion control: slow start, AIMD, ssthresh — walk through it.", "hint": "exponential growth, sawtooth, multiplicative decrease"},
    {"q": "TCP timeout vs fast retransmit — difference?", "hint": "RTO expiry vs three dupACKs"},
    {"q": "What is head-of-line blocking in TCP and how does HTTP/2 multiplexing relate?", "hint": "one lost segment stalls all streams"},
    {"q": "NAT: how does it work and what breaks because of it?", "hint": "port translation, inbound connections, hole punching"},
    {"q": "DHCP: what does DORA stand for and why is it UDP broadcast?", "hint": "discover, offer, request, ack"},
    {"q": "ARP spoofing — attack and defense.", "hint": "fake MAC mapping, dynamic ARP inspection"},
    {"q": "IPv4 exhaustion: how did NAT, CIDR and IPv6 each help?", "hint": "32-bit space, prefix aggregation, 128-bit"},
    {"q": "Routing: distance-vector vs link-state.", "hint": "count-to-infinity vs Dijkstra flooding"},
    {"q": "What is BGP and why is it called the protocol that runs the internet?", "hint": "AS peering, policy over shortest path"},
    {"q": "DNS recursion vs iteration; what does a resolver actually do?", "hint": "root, TLD, authoritative chain, caching"},
    {"q": "DNS over UDP port 53 vs DoH/DoT — why the shift?", "hint": "privacy, tampering, firewall traversal"},
    {"q": "TCP vs TLS handshake ordering: how many round trips to first byte in HTTPS?", "hint": "TCP SYN + TLS hello, resumption saves RTTs"},
    {"q": "Symmetric vs asymmetric encryption — why does TLS use both?", "hint": "key exchange + certificates, bulk data cipher"},
    {"q": "What does a certificate chain prove and who anchors trust?", "hint": "root CAs, signature verification, hostname match"},
    {"q": "Forward secrecy: what property do ephemeral Diffie-Hellman keys give?", "hint": "past captures stay encrypted even if key leaks"},
    {"q": "HTTP/1.1 keep-alive vs pipelining vs HTTP/2 streams vs HTTP/3 QUIC.", "hint": "evolution of multiplexing and transport"},
    {"q": "Cookies vs sessions vs JWT — tradeoffs.", "hint": "server state, signed tokens, revocation pain"},
    {"q": "CORS: what is the browser actually enforcing?", "hint": "same-origin policy, preflight OPTIONS, server opt-in"},
    {"q": "Idempotency in HTTP methods: why does it matter for retries?", "hint": "GET/PUT/DELETE idempotent, POST not, idempotency keys"},
    {"q": "Long polling vs WebSockets vs SSE.", "hint": "direction, overhead, proxy friendliness"},
    {"q": "CDN: how does edge caching decide what to serve and when to revalidate?", "hint": "TTL, ETag, If-None-Match, geo-routing"},
    {"q": "Load balancer algorithms: round robin, least connections, consistent hashing.", "hint": "session stickiness, cache locality on scale events"},
    {"q": "What is a socket? Differentiate socket pairs, ports and file descriptors.", "hint": "4-tuple identity, fd table entry"},
    {"q": "C10K problem: how do epoll/kqueue solve it vs thread-per-connection?", "hint": "event loops, readiness notification, amortized cost"},
    {"q": "Zero-copy sendfile: what copies does it eliminate?", "hint": "kernel-to-user round trips, NIC scatter-gather"},
    {"q": "MTU and fragmentation: why does VPN/tunneling cause black holes?", "hint": "PMTUD, DF bit, MSS clamping"},
    {"q": "Checksums exist at multiple layers — why isn't one layer enough?", "hint": "end-to-end argument, hops corrupt independently"},
    {"q": "What is a DDoS amplification attack and which protocols get abused?", "hint": "DNS/NTP reflection, spoofed source IP"},
    {"q": "SYN flood and SYN cookies — how does the defense work?", "hint": "stateless encoding of connection info"},
    {"q": "Man-in-the-middle on public wifi: exact attack steps and mitigations.", "hint": "arp spoof + ssl strip; HSTS, cert pinning"},
    {"q": "Traceroute: how does TTL exploitation reveal the path?", "hint": "ICMP time exceeded per hop"},
    {"q": "Quality of Service: how would you prioritize a video call on a congested link?", "hint": "queueing disciplines, DSCP marking, jitter buffers"},
    {"q": "Latency vs bandwidth vs throughput — and why ping alone misleads.", "hint": "RTT, capacity, achieved rate, bdp math"},
    {"q": "Why do cloud cross-AZ transfers cost money and add ~1ms? Design around it.", "hint": "physical distance, replication factor, locality"}
  ],
  "DBMS": [
    {"q": "Relational algebra basics: selection, projection, join — map to SQL.", "hint": "WHERE, SELECT cols, JOIN"},
    {"q": "Primary key vs unique key vs foreign key.", "hint": "nullability, referential integrity enforcement"},
    {"q": "Normalization: 1NF through BCNF with one example each.", "hint": "atomicity, partial dependency, transitive dependency"},
    {"q": "When would you deliberately denormalize?", "hint": "read-heavy analytics, join elimination, materialized views"},
    {"q": "ACID: define each property and what breaks without it.", "hint": "transfer example for atomicity/durability"},
    {"q": "Isolation levels: read committed, repeatable read, serializable — anomalies prevented by each.", "hint": "dirty reads, non-repeatable reads, phantoms"},
    {"q": "Lost update problem: show it with two concurrent transactions.", "hint": "read-modify-write interleave, row locks or optimistic retry"},
    {"q": "Pessimistic vs optimistic concurrency control.", "hint": "lock-first vs validate-at-commit, conflict rates"},
    {"q": "Two-phase locking: how does it guarantee serializability?", "hint": "growing phase, shrinking phase, deadlock window"},
    {"q": "MVCC: how do readers never block writers?", "hint": "version chains, snapshot at txn start, GC of old versions"},
    {"q": "Write-ahead logging: why must the log hit disk before the page?", "hint": "undo/redo recovery, steal/no-force policies"},
    {"q": "What does COMMIT durability actually guarantee on power loss?", "hint": "fsync of WAL, group commit, doublewrite"},
    {"q": "B-tree vs B+tree vs LSM-tree indexes.", "hint": "in-node data, leaf chains, write amplification vs read amp"},
    {"q": "Clustered vs non-clustered (secondary) index.", "hint": "row order = index order, pointer vs payload"},
    {"q": "Covering index: how does it skip the table entirely?", "hint": "all queried columns in index, index-only scan"},
    {"q": "When does the optimizer choose a full scan over your index?", "hint": "selectivity, cardinality estimates, function on column"},
    {"q": "EXPLAIN plan reading: spot a missing-index join by shape.", "hint": "nested loop with filter, high rows estimate"},
    {"q": "Index selectivity and composite column order — which goes first?", "hint": "equality columns, range last, cardinality"},
    {"q": "N+1 query problem: detect and fix.", "hint": "loop of selects, batch IN or JOIN, ORM lazy loading"},
    {"q": "Pagination: OFFSET vs keyset/cursor — performance at page 10000.", "hint": "scan-and-discard vs seek predicate"},
    {"q": "Lock granularity: row, page, table — escalation tradeoffs.", "hint": "overhead vs contention, lock escalation triggers"},
    {"q": "Deadlock detection in databases: who wins and who aborts?", "hint": "waits-for graph cycle, victim selection cost"},
    {"q": "Phantom reads: how does next-key/predicate locking stop them?", "hint": "gap locks, range locks in InnoDB RR"},
    {"q": "CAP theorem: pick two — what do real distributed DBs actually do?", "hint": "partition tolerance forced, tunable C/A"},
    {"q": "Eventual consistency: how do quorum reads/writes bound staleness?", "hint": "W+R>N, read repair, hinted handoff"},
    {"q": "Raft consensus in five sentences.", "hint": "leader election, log replication, commit rules, safety terms"},
    {"q": "Sharding strategies: range vs hash vs directory.", "hint": "hotspots vs rebalancing cost vs flexibility"},
    {"q": "Rebalancing a hash-sharded cluster without downtime — consistent hashing's role.", "hint": "virtual nodes move small slices"},
    {"q": "Replication: synchronous vs semi-sync vs async failover behavior.", "hint": "data loss windows, RPO, split brain fencing"},
    {"q": "Read replicas: what can go wrong serving reads from lagging followers?", "hint": "read-your-writes violations, monotonic reads"},
    {"q": "OLTP vs OLAP: storage layout differences (row vs column).", "hint": "point lookups vs aggregate scans, compression"},
    {"q": "Star schema vs snowflake schema.", "hint": "denormalized dims vs normalized, join cost"},
    {"q": "Materialized views: freshness vs speed, how do you refresh?", "hint": "eager incremental vs scheduled rebuild"},
    {"q": "Connection pooling: why is opening a new DB connection expensive?", "hint": "TCP+auth+backend process, pool sizing math"},
    {"q": "Transaction isolation vs database-level advisory locks for job schedulers.", "hint": "SELECT FOR UPDATE SKIP LOCKED pattern"},
    {"q": "Soft delete vs hard delete: query and compliance implications.", "hint": "deleted_at filters everywhere, GDPR purge jobs"},
    {"q": "UUID v4 as primary key: what breaks in a B-tree and what fixes it?", "hint": "random inserts, page splits; UUIDv7/time-ordered"},
    {"q": "Schema migration with zero downtime: expand-contract pattern.", "hint": "add nullable, backfill, dual-read, drop old"},
    {"q": "Data corruption bug shipped: how do backups, PITR and WAL replay save you?", "hint": "base backup + WAL to point-in-time"},
    {"q": "Design a URL shortener's schema: hot reads, dedupe, analytics.", "hint": "hash collision handling, counter tables, TTL"}
  ]
}
